# Week 08: API Dockerfile
# ========================
# นักศึกษาต้องทำตาม TODO ที่กำหนด

# TODO 1: กำหนด base image (5 คะแนน)
# ใช้ Python 3.11 slim image
# Hint: FROM python:3.11-slim
#
# เขียนโค้ดด้านล่างนี้:
FROM TODO:version

# Set working directory
WORKDIR /app

# TODO 2: Copy และ install requirements (10 คะแนน)
# ขั้นตอน:
# 1. Copy requirements.txt ก่อน (เพื่อใช้ Docker cache)
# 2. Run pip install
#
# Hint:
# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt
#
# เขียนโค้ดด้านล่างนี้:


# TODO 3: Copy source code (5 คะแนน)
# Copy ไฟล์ทั้งหมดใน directory ปัจจุบัน
#
# Hint: COPY . .
#
# เขียนโค้ดด้านล่างนี้:


# Expose port
EXPOSE 9000

# TODO 4: กำหนด CMD สำหรับรัน FastAPI (5 คะแนน)
# รัน uvicorn กับ main:app
# - host: 0.0.0.0
# - port: 9000
#
# Hint: CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "9000"]
#
# เขียนโค้ดด้านล่างนี้:
CMD ["echo", "TODO: Replace this CMD"]
